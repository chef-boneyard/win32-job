= Description
  A Ruby interface to Windows' jobs, process groups for Windows.

= Synopsis
  require 'win32/job'
  include Win32

  job = Job.create('test')

  job.add_process(1234)
  job.add_process(1235)

  job.close
